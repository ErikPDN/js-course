const name = "Erik Pereira";
let years = 20;
let weight = 72.5;
const height = 1.68;

let bmi = (weight / Math.pow(height, 2))
console.log(`${name} is ${years} years old, he weighs ${weight}`)
console.log(`he is ${height} tall and his BMI is ${bmi}`)